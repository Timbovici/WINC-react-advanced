Hoe te starten:

1. npm install

2. npx json-server --watch events.json --port 3000

3. npm run dev

4. Omgeving:
   Zorg dat je een `.env` bestand in de hoofdmap hebt (naast package.json) met daarin:
   VITE_API_BASE_URL="http://localhost:3000"